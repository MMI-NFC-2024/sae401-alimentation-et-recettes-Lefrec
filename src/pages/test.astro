---
import { getRecette, getRecetteMinimal, getRecettes, getProfessionnels, getProfessionnel } from "../../backend/functions"

const results = await getRecettes({
  search: "cakes"
});

console.log(results)

// const results = await getProfessionnels({
//     professions: ['Diététicien'],
// });

// console.log(results);

---
