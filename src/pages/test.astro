---
import { getRecette, getRecetteMinimal, getRecettes } from "../../backend/functions"

const results = await getRecettes({
  tags: ['Végétarien'],
  niveau: 'débutant',
  maxTemps: 30,
  minNote: 3.5,
  ingredients: ['x3dblfljnxnyhof'],
});

console.log(results)
---
